#include //placeholder here

int appIter(unsigned char* inputBuf, unsigned char* outputBuf, unsigned char* outputChunk
          shaSoc){
          
          unsigned char* ShaTarg = somearraythingIneedtothinkabout; //Just pass with copy for now
          
  *newChunkIP, newChunkLen = CDC(someinputoftheabove); // We need to know if we have a new chunk or not. If so, let's go / we need init point and length of new chunk
  
  if(NEWCHUNK){
    compute_sha3_hash(shaSoc, newChunkIP, newChunkLen,  ShaTarg); // Hash whole chunk. Really it is better if we did this incrementally
    checkMatch(matchID,
    if (matchID==0) { //If we match
    sendDupChunk(matchID, outputBuf)
    }
    else{
    //Theoretically run these concurrent
    opBufPrep(outputBuf, newChunkLen); (
    LZW(newChunkIP, newChunkLen, outputBuf); //Start writing to out
    }
  }
}
